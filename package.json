{
  "name": "armanriazi-etereum-election",
  "version": "1.0.0",
  "description": "arazhit.ir",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "lite-server & set PORT=8000 ",
    "start-dep": "concurrently --kill-others \"npm  lite-server\" \"npm ganache-cli --host 0.0.0.0\"",
    "start-dev": "concurrently --kill-others \"npm  run dev\" \"npm run ganache\"",
    "ganache": "ganache-cli --host 0.0.0.0",
    "truffle-console": "truffle console",
    "truffle-test": "truffle test",
    "truffle-network": "truffle network",
    "truffle-version": "truffle version",
    "truffle-network-clean": "truffle networks --clean",
    "truffle-migrate-reset": "truffle migrate --reset",
    "truffle-migrate": "truffle migrate",
    "truffle-compile": "truffle compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "armanriazi",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "ganache-cli": "^6.12.1",
    "lite-server": "^2.6.1"
  },
  "dependencies": {
    "truffle": "^4.1.0"
  }
}
